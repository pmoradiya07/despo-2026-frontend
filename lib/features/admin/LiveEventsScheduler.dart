import 'package:flutter/material.dart';

class LiveEventScheduler extends StatelessWidget{
  const LiveEventScheduler({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}